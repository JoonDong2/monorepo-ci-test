{
  "name": "monorepo-ci-test",
  "version": "0.0.0",
  "description": ".",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changeset:version": "changeset version"
  },
  "author": "Joondong2",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@4.7.0",
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.28.1"
  }
}
